# HW-PVT-HSS

usage: HW-PVT-HSS [-h] [-t TIME]

A simple script with a list option.

optional arguments:
  -h, --help            show this help message and exit
  -t TIME, --time TIME  test time. unit: second, min 300, default 3600

-h 显示帮助
-t 测试时间，单位（秒）, 默认3600s， 最小300s

eg.
  HW-PVT-HSS -t 300
